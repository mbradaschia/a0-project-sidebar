"""Installation script for project_sidebar plugin.

This plugin is frontend-only (Alpine.js store + CSS extension).
No additional packages or setup steps are required.
"""

import sys


def main() -> int:
    """No-op install — plugin requires no external dependencies."""
    return 0


if __name__ == "__main__":
    sys.exit(main())
